impl Vec<u64> {
    fn repair_cars(&self, cars: u64) -> u128 {
        let lower_bound: u128 = 0;
        let upper_bound: u128 = self.iter().min().unwrap() as u128;
    }
}

fn main() {
    let ranks: Vec<u64> = Vec::new();
    let mut ranks = vec![4, 3, 2, 1];
    let cars: u64 = 10;
}
